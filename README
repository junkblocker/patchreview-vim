1) Copy patchreview.vim to your ~/.vim/plugin directory, patchreview.txt to
   your ~/.vim/doc directory and patchreviewlib.vim to your ~/.vim/autoload
   directory.

2) Open vim and do

   :cd ~/.vim/doc
   :helptags .
